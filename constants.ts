import { Category } from './types';

export const CATEGORIES: Category[] = [
  'Food',
  'Transport',
  'Entertainment',
  'Housing',
  'Utilities',
  'Health',
  'Shopping',
  'Other'
];

export const DEFAULT_ALERT_THRESHOLD = 80; // 80%

// Mock user for "Splitwise" features
export const CURRENT_USER = "Me";
